# AI History Log

## 2026-07-13

- App version is now `1.0.0` and is displayed globally from `package.json`.
- For every future AI-made app change, update the app version in `package.json` and `package-lock.json` according to the size of the change so the visible version stays current.
